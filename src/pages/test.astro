---
import { supabase } from '../lib/supabase';

const { data, error } = await supabase.from("todos").select('*');

if(error) {
    console.log(error)
}

console.log(data)
---

{
  (
    <ul>
      {data.map((entry:any) => (
        <li>{entry.name}</li>
      ))}
    </ul>
  <h1>hi</h1>
  )
}
